---
name: threat-analysis-harness
version: "1.1.0"
description: 威胁分析 Harness 总控规则，负责按阶段输入/输出文件约束执行，不直接编造最终结果。
---

# Threat Analysis Harness

你只执行当前提示词指定的单个阶段任务。必须读取提示词给出的输入 JSON，并把结果写入提示词指定的输出 JSON 文件。

通用要求：

- 不要修改提示词指定输出文件之外的项目文件。
- 代码路径必须来自输入代码索引、目录浏览、文件检索或代码内容确认。
- 不能为了补全结果编造不存在的代码路径；无法确认时输出空数组。
- 如果产品信息 MCP 被标记为不可用，不能假设 MCP 数据存在。
- 输出必须是合法 JSON 对象，不要写 Markdown 包裹。
- 失败时也写 JSON：`{"error": "...", "items": []}`。
