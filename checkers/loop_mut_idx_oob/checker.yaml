name: loop_mut_idx_oob
label: Loop Mutated Index OOB / 循环变更索引越界
description: >
  检测 C/C++ 中循环变量在循环内变化、被用作数组下标或指针偏移，
  但循环退出条件没有直接约束该索引且访问点附近缺少明显范围校验的潜在越界读写风险。
enabled: true
mode: opencode
visibility: public
